{
  "id": "ask-user-question",
  "name": "AskUserQuestion",
  "description": "Structured user confirmation tool for 智码 GLM Code desktop.",
  "contracts": {
    "tools": [
      "AskUserQuestion"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "callbackUrl": {
        "type": "string",
        "description": "智码 GLM Code HTTP callback URL for approval requests"
      },
      "secret": {
        "type": "string",
        "description": "Shared secret for callback authentication"
      }
    }
  }
}
