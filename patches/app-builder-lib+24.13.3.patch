diff --git a/node_modules/app-builder-lib/out/targets/nsis/WebInstallerTarget.js b/node_modules/app-builder-lib/out/targets/nsis/WebInstallerTarget.js
index 9b75baa..e018ea7 100644
--- a/node_modules/app-builder-lib/out/targets/nsis/WebInstallerTarget.js
+++ b/node_modules/app-builder-lib/out/targets/nsis/WebInstallerTarget.js
@@ -23,8 +23,11 @@ class WebInstallerTarget extends NsisTarget_1.NsisTarget {
                 throw new Error("Cannot compute app package download URL");
             }
             appPackageUrl = (0, PublishManager_1.computeDownloadUrl)(publishConfigs[0], null, packager);
+            // Computed URLs point at a directory, so the NSIS template appends the
+            // package file name. An explicit appPackageUrl is used verbatim, as the
+            // option documentation states ("it is full URL").
+            defines.APP_PACKAGE_URL_IS_INCOMPLETE = null;
         }
-        defines.APP_PACKAGE_URL_IS_INCOMPLETE = null;
         defines.APP_PACKAGE_URL = appPackageUrl;
     }
     get installerFilenamePattern() {
diff --git a/node_modules/app-builder-lib/templates/nsis/assistedInstaller.nsh b/node_modules/app-builder-lib/templates/nsis/assistedInstaller.nsh
index b7f4752..e11b58f 100644
--- a/node_modules/app-builder-lib/templates/nsis/assistedInstaller.nsh
+++ b/node_modules/app-builder-lib/templates/nsis/assistedInstaller.nsh
@@ -43,6 +43,18 @@
     !insertmacro customPageAfterChangeDir
   !endif

+  # In-app update (--updated): the previous install directory may already be
+  # renamed away by customCheckAppRunning, so a mid-install cancel would leave
+  # the user with no working app at all. Disable the Cancel button for the
+  # whole install-files page in that mode.
+  Function instFilesShow
+    ${if} ${isUpdated}
+      GetDlgItem $0 $HWNDPARENT 2
+      EnableWindow $0 0
+    ${endif}
+  FunctionEnd
+  !define MUI_PAGE_CUSTOMFUNCTION_SHOW instFilesShow
+
   !insertmacro MUI_PAGE_INSTFILES
   !ifmacrodef customFinishPage
     !insertmacro customFinishPage
@@ -60,6 +72,9 @@
       !define MUI_FINISHPAGE_RUN
       !define MUI_FINISHPAGE_RUN_FUNCTION "StartApp"
     !endif
+    # In-app update: no finish page either -- the app is relaunched by the
+    # install section itself (isForceRun), so the update ends hands-free.
+    !insertmacro skipPageIfUpdated
     !insertmacro MUI_PAGE_FINISH
   !endif
 !else
diff --git a/node_modules/app-builder-lib/templates/nsis/include/extractAppPackage.nsh b/node_modules/app-builder-lib/templates/nsis/include/extractAppPackage.nsh
index f5470bf..c9ebc07 100644
--- a/node_modules/app-builder-lib/templates/nsis/include/extractAppPackage.nsh
+++ b/node_modules/app-builder-lib/templates/nsis/include/extractAppPackage.nsh
@@ -66,24 +66,46 @@
 !macroend

 !macro arm64_app_files
+  !ifmacrodef customAppPackageMaterializeStart
+    !insertmacro customAppPackageMaterializeStart
+  !endif
   File /oname=$PLUGINSDIR\app-arm64.${COMPRESSION_METHOD} "${APP_ARM64}"
+  !ifmacrodef customAppPackageMaterializeEnd
+    !insertmacro customAppPackageMaterializeEnd
+  !endif
 !macroend

 !macro x64_app_files
+  !ifmacrodef customAppPackageMaterializeStart
+    !insertmacro customAppPackageMaterializeStart
+  !endif
   File /oname=$PLUGINSDIR\app-64.${COMPRESSION_METHOD} "${APP_64}"
+  !ifmacrodef customAppPackageMaterializeEnd
+    !insertmacro customAppPackageMaterializeEnd
+  !endif
 !macroend

 !macro ia32_app_files
+  !ifmacrodef customAppPackageMaterializeStart
+    !insertmacro customAppPackageMaterializeStart
+  !endif
   File /oname=$PLUGINSDIR\app-32.${COMPRESSION_METHOD} "${APP_32}"
+  !ifmacrodef customAppPackageMaterializeEnd
+    !insertmacro customAppPackageMaterializeEnd
+  !endif
 !macroend

 !macro decompress
   !ifdef ZIP_COMPRESSION
     nsisunz::Unzip "$PLUGINSDIR\app-$packageArch.zip" "$INSTDIR"
     Pop $R0
-    StrCmp $R0 "success" +3
+    ${if} $R0 != "success"
       MessageBox MB_OK|MB_ICONEXCLAMATION "$(decompressionFailed)$\n$R0"
+      !ifmacrodef customBeforeInstallerQuit
+        !insertmacro customBeforeInstallerQuit "zip-decompression-failed"
+      !endif
       Quit
+    ${endIf}
   !else
     !insertmacro extractUsing7za "$PLUGINSDIR\app-$packageArch.7z"
   !endif
@@ -94,8 +116,15 @@
   CreateDirectory "$PLUGINSDIR\7z-out"
   ClearErrors
   SetOutPath "$PLUGINSDIR\7z-out"
+  !ifmacrodef customAppPackageExtractStart
+    !insertmacro customAppPackageExtractStart "staging" "${FILE}"
+  !endif
   Nsis7z::Extract "${FILE}"
   Pop $R0
+  !ifmacrodef customAppPackageExtractEnd
+    # Nsis7z does not push a result. $R0 is the saved OUTDIR restored below.
+    !insertmacro customAppPackageExtractEnd "staging" "unchecked"
+  !endif
   SetOutPath $R0

   # Retry counter
@@ -105,8 +134,23 @@
     IntOp $R1 $R1 + 1

     # Attempt to copy files in atomic way
+    !ifmacrodef customAppPackageCopyStart
+      !insertmacro customAppPackageCopyStart
+    !endif
+    # Timing/log hooks may touch NSIS' error flag. Only CopyFiles itself may
+    # decide whether this attempt enters the retry path.
+    ClearErrors
     CopyFiles /SILENT "$PLUGINSDIR\7z-out\*" $OUTDIR
-    IfErrors 0 DoneExtract7za
+    IfErrors CopyExtract7zaFailed
+    !ifmacrodef customAppPackageCopyEnd
+      !insertmacro customAppPackageCopyEnd "success"
+    !endif
+    Goto DoneExtract7za
+
+    CopyExtract7zaFailed:
+    !ifmacrodef customAppPackageCopyEnd
+      !insertmacro customAppPackageCopyEnd "error"
+    !endif

     DetailPrint `Can't modify "${PRODUCT_NAME}"'s files.`
     ${if} $R1 < 5
@@ -124,10 +168,19 @@
     # much disk space.
     RMDir /r "$PLUGINSDIR\7z-out"

+    !ifmacrodef customAppPackageExtractStart
+      !insertmacro customAppPackageExtractStart "fallback-direct" "${FILE}"
+    !endif
     Nsis7z::Extract "${FILE}"
+    !ifmacrodef customAppPackageExtractEnd
+      !insertmacro customAppPackageExtractEnd "fallback-direct" "unchecked"
+    !endif
     Goto DoneExtract7za

   AbortExtract7za:
+    !ifmacrodef customBeforeInstallerQuit
+      !insertmacro customBeforeInstallerQuit "payload-copy-aborted"
+    !endif
     Quit

   RetryExtract7za:
diff --git a/node_modules/app-builder-lib/templates/nsis/include/installer.nsh b/node_modules/app-builder-lib/templates/nsis/include/installer.nsh
index 34e91df..702be81 100644
--- a/node_modules/app-builder-lib/templates/nsis/include/installer.nsh
+++ b/node_modules/app-builder-lib/templates/nsis/include/installer.nsh
@@ -79,7 +79,13 @@
           SetShellVarContext current
         ${endif}

+        !ifmacrodef customInstallerCacheCopyStart
+          !insertmacro customInstallerCacheCopyStart "package"
+        !endif
         !insertmacro moveFile "$packageFile" "$LOCALAPPDATA\${APP_PACKAGE_STORE_FILE}"
+        !ifmacrodef customInstallerCacheCopyEnd
+          !insertmacro customInstallerCacheCopyEnd "package" "completed"
+        !endif

         ${if} $installMode == "all"
           SetShellVarContext all
@@ -90,7 +96,18 @@
       ${if} $installMode == "all"
         SetShellVarContext current
       ${endif}
+      !ifmacrodef customInstallerCacheCopyStart
+        !insertmacro customInstallerCacheCopyStart "installer"
+      !endif
       !insertmacro copyFile "$EXEPATH" "$LOCALAPPDATA\${APP_INSTALLER_STORE_FILE}"
+      !ifmacrodef customInstallerCacheCopyEnd
+        IfErrors InstallerCacheCopyFailed
+        !insertmacro customInstallerCacheCopyEnd "installer" "success"
+        Goto InstallerCacheCopyDone
+        InstallerCacheCopyFailed:
+        !insertmacro customInstallerCacheCopyEnd "installer" "error"
+        InstallerCacheCopyDone:
+      !endif
       ${if} $installMode == "all"
         SetShellVarContext all
       ${endif}
@@ -138,9 +155,18 @@

   # allow user to define ESTIMATED_SIZE to avoid GetSize call
   !ifdef ESTIMATED_SIZE
+    !ifmacrodef customEstimatedSizeKnown
+      !insertmacro customEstimatedSizeKnown "${ESTIMATED_SIZE}"
+    !endif
     IntFmt $0 "0x%08X" ${ESTIMATED_SIZE}
   !else
+    !ifmacrodef customEstimatedSizeScanStart
+      !insertmacro customEstimatedSizeScanStart
+    !endif
     ${GetSize} "$INSTDIR" "/S=0K" $0 $1 $2
+    !ifmacrodef customEstimatedSizeScanEnd
+      !insertmacro customEstimatedSizeScanEnd "$0"
+    !endif
     IntFmt $0 "0x%08X" $0
   !endif

diff --git a/node_modules/app-builder-lib/templates/nsis/include/webPackage.nsh b/node_modules/app-builder-lib/templates/nsis/include/webPackage.nsh
index 8ec0be3..253545c 100644
--- a/node_modules/app-builder-lib/templates/nsis/include/webPackage.nsh
+++ b/node_modules/app-builder-lib/templates/nsis/include/webPackage.nsh
@@ -39,26 +39,44 @@
     StrCpy $packageArch "32"
   ${endif}

+  # The page's own progress bar (IDC_PROGRESS = 1004) stays frozen during the
+  # plugin call, so hide it while downloading: the inetc panel is then the only
+  # progress bar on the page. Restored after the transfer for the install phase.
+  FindWindow $R8 "#32770" "" $HWNDPARENT
+  GetDlgItem $R9 $R8 1004
+  ShowWindow $R9 0
+
   download:
-  inetc::get /USERAGENT "electron-builder (Mozilla)" /HEADER "X-Arch: $packageArch" /RESUME "" "$packageUrl" "$PLUGINSDIR\package.7z" /END
+  inetc::get /USERAGENT "electron-builder (Mozilla)" /HEADER "X-Arch: $packageArch" /CANCELTEXT "${U+53D6}${U+6D88}" /QUESTION "${U+786E}${U+5B9A}${U+8981}${U+53D6}${U+6D88}${U+4E0B}${U+8F7D}${U+5417}${U+FF1F}" /RESUME "${U+7F51}${U+7EDC}${U+8FDE}${U+63A5}${U+4E2D}${U+65AD}${U+FF0C}${U+8BF7}${U+68C0}${U+67E5}${U+7F51}${U+7EDC}${U+540E}${U+70B9}${U+51FB}${U+201C}${U+91CD}${U+8BD5}${U+201D}${U+7EE7}${U+7EED}${U+4E0B}${U+8F7D}${U+3002}" /TRANSLATE "${U+6B63}${U+5728}${U+4E0B}${U+8F7D} 智码 GLM Code ${U+5B89}${U+88C5}${U+5305}" "${U+6B63}${U+5728}${U+8FDE}${U+63A5}${U+670D}${U+52A1}${U+5668}${U+2026}" "${U+79D2}" "${U+5206}${U+949F}" "${U+5C0F}${U+65F6}" "" "${U+5DF2}${U+4E0B}${U+8F7D} %dkB${U+FF08}%d%%${U+FF09}${U+FF0C}${U+5171} %dkB ${U+2014} %d.%01dkB/s" "${U+FF0C}${U+5269}${U+4F59}${U+7EA6} %d %s" "$packageUrl" "$PLUGINSDIR\package.7z" /END
   Pop $0

   ${if} $0 == "Cancelled"
+    !ifmacrodef customBeforeInstallerQuit
+      !insertmacro customBeforeInstallerQuit "web-package-download-cancelled"
+    !endif
     Quit
   ${endif}

   ${if} $0 != "OK"
     # try without proxy
-    inetc::get /NOPROXY /USERAGENT "electron-builder (Mozilla)" /HEADER "X-Arch: $packageArch" /RESUME "" "$packageUrl" "$PLUGINSDIR\package.7z" /END
+    inetc::get /NOPROXY /USERAGENT "electron-builder (Mozilla)" /HEADER "X-Arch: $packageArch" /CANCELTEXT "${U+53D6}${U+6D88}" /QUESTION "${U+786E}${U+5B9A}${U+8981}${U+53D6}${U+6D88}${U+4E0B}${U+8F7D}${U+5417}${U+FF1F}" /RESUME "${U+7F51}${U+7EDC}${U+8FDE}${U+63A5}${U+4E2D}${U+65AD}${U+FF0C}${U+8BF7}${U+68C0}${U+67E5}${U+7F51}${U+7EDC}${U+540E}${U+70B9}${U+51FB}${U+201C}${U+91CD}${U+8BD5}${U+201D}${U+7EE7}${U+7EED}${U+4E0B}${U+8F7D}${U+3002}" /TRANSLATE "${U+6B63}${U+5728}${U+4E0B}${U+8F7D} 智码 GLM Code ${U+5B89}${U+88C5}${U+5305}" "${U+6B63}${U+5728}${U+8FDE}${U+63A5}${U+670D}${U+52A1}${U+5668}${U+2026}" "${U+79D2}" "${U+5206}${U+949F}" "${U+5C0F}${U+65F6}" "" "${U+5DF2}${U+4E0B}${U+8F7D} %dkB${U+FF08}%d%%${U+FF09}${U+FF0C}${U+5171} %dkB ${U+2014} %d.%01dkB/s" "${U+FF0C}${U+5269}${U+4F59}${U+7EA6} %d %s" "$packageUrl" "$PLUGINSDIR\package.7z" /END
     Pop $0
   ${endif}

   ${if} $0 == "Cancelled"
+    !ifmacrodef customBeforeInstallerQuit
+      !insertmacro customBeforeInstallerQuit "web-package-download-cancelled"
+    !endif
     quit
   ${elseif} $0 != "OK"
-    Messagebox MB_RETRYCANCEL|MB_ICONEXCLAMATION "Unable to download application package from $packageUrl (status: $0).$\r$\n$\r$\nPlease check your internet connection and retry." IDRETRY download
+    Messagebox MB_RETRYCANCEL|MB_ICONEXCLAMATION "${U+4E0B}${U+8F7D}${U+5B89}${U+88C5}${U+7EC4}${U+4EF6}${U+5931}${U+8D25}${U+FF08}${U+72B6}${U+6001}${U+FF1A}$0${U+FF09}${U+3002}$\r$\n$\r$\n${U+8BF7}${U+68C0}${U+67E5}${U+7F51}${U+7EDC}${U+8FDE}${U+63A5}${U+540E}${U+70B9}${U+51FB}${U+201C}${U+91CD}${U+8BD5}${U+201D}${U+3002}" IDRETRY download
+    !ifmacrodef customBeforeInstallerQuit
+      !insertmacro customBeforeInstallerQuit "web-package-download-failed"
+    !endif
     Quit
   ${endif}

+  ShowWindow $R9 5
+
   StrCpy $packageFile "$PLUGINSDIR\package.7z"
 !macroend
diff --git a/node_modules/app-builder-lib/templates/nsis/installSection.nsh b/node_modules/app-builder-lib/templates/nsis/installSection.nsh
index 053772f..57a73d8 100644
--- a/node_modules/app-builder-lib/templates/nsis/installSection.nsh
+++ b/node_modules/app-builder-lib/templates/nsis/installSection.nsh
@@ -11,6 +11,20 @@ StrCpy $appExe "$INSTDIR\${APP_EXECUTABLE_FILENAME}"
 # must be called before uninstallOldVersion
 !insertmacro setLinkVars

+# Preserve the user's shortcut choices while the old app executable still
+# exists. A custom update fast path may rename $INSTDIR in CHECK_APP_RUNNING.
+Var /GLOBAL keepShortcuts
+StrCpy $keepShortcuts "false"
+!insertMacro setIsTryToKeepShortcuts
+${if} $isTryToKeepShortcuts == "true"
+  ReadRegStr $R1 SHELL_CONTEXT "${INSTALL_REGISTRY_KEY}" KeepShortcuts
+
+  ${if} $R1 == "true"
+  ${andIf} ${FileExists} "$appExe"
+    StrCpy $keepShortcuts "true"
+  ${endIf}
+${endif}
+
 !ifdef ONE_CLICK
   !ifdef HEADER_ICO
     File /oname=$PLUGINSDIR\installerHeaderico.ico "${HEADER_ICO}"
@@ -37,26 +51,26 @@ StrCpy $appExe "$INSTDIR\${APP_EXECUTABLE_FILENAME}"
   ${endif}
 !endif

-Var /GLOBAL keepShortcuts
-StrCpy $keepShortcuts "false"
-!insertMacro setIsTryToKeepShortcuts
-${if} $isTryToKeepShortcuts == "true"
-  ReadRegStr $R1 SHELL_CONTEXT "${INSTALL_REGISTRY_KEY}" KeepShortcuts
-
-  ${if} $R1 == "true"
-  ${andIf} ${FileExists} "$appExe"
-    StrCpy $keepShortcuts "true"
-  ${endIf}
-${endif}
-
-!insertmacro uninstallOldVersion SHELL_CONTEXT
-!insertmacro handleUninstallResult SHELL_CONTEXT
+!ifmacrodef customUninstallOldVersion
+  !insertmacro customUninstallOldVersion SHELL_CONTEXT
+!else
+  !insertmacro uninstallOldVersion SHELL_CONTEXT
+  !insertmacro handleUninstallResult SHELL_CONTEXT
+!endif

 ${if} $installMode == "all"
-  !insertmacro uninstallOldVersion HKEY_CURRENT_USER
-  !insertmacro handleUninstallResult HKEY_CURRENT_USER
+  !ifmacrodef customUninstallOldVersion
+    !insertmacro customUninstallOldVersion HKEY_CURRENT_USER
+  !else
+    !insertmacro uninstallOldVersion HKEY_CURRENT_USER
+    !insertmacro handleUninstallResult HKEY_CURRENT_USER
+  !endif
 ${endIf}

+!ifmacrodef customAfterUninstallOldVersions
+  !insertmacro customAfterUninstallOldVersions
+!endif
+
 SetOutPath $INSTDIR

 !ifdef UNINSTALLER_ICON
@@ -66,2 +80,5 @@
 !insertmacro installApplicationFiles
+!ifmacrodef customBeforeRegistryAddInstallInfo
+  !insertmacro customBeforeRegistryAddInstallInfo
+!endif
 !insertmacro registryAddInstallInfo
@@ -102,9 +119,13 @@ ${endIf}
   !endif
   !insertmacro quitSuccess
 !else
-  # for assisted installer run only if silent, because assisted installer has run after finish option
+  # for assisted installer run only if silent or updated, because otherwise the
+  # assisted installer has its run-after-finish option; in updated mode the
+  # finish page is skipped, so this is what relaunches the app.
   ${if} ${isForceRun}
-  ${andIf} ${Silent}
-    !insertmacro doStartApp
+    ${if} ${Silent}
+    ${orIf} ${isUpdated}
+      !insertmacro doStartApp
+    ${endIf}
   ${endIf}
 !endif
diff --git a/node_modules/app-builder-lib/templates/nsis/installer.nsi b/node_modules/app-builder-lib/templates/nsis/installer.nsi
index 1b88a9d..a52559b 100644
--- a/node_modules/app-builder-lib/templates/nsis/installer.nsi
+++ b/node_modules/app-builder-lib/templates/nsis/installer.nsi
@@ -33,8 +33,24 @@ Var oldMenuDirectory
   !include "assistedInstaller.nsh"
 !endif

+!ifmacrodef customInstallerUserAbort
+  !define MUI_CUSTOMFUNCTION_ABORT lobsterCustomInstallerUserAbort
+!endif
+
 !insertmacro addLangs

+!ifmacrodef customInstallerUserAbort
+  Function lobsterCustomInstallerUserAbort
+    !insertmacro customInstallerUserAbort
+  FunctionEnd
+!endif
+
+!ifmacrodef customInstallerFailed
+  Function .onInstFailed
+    !insertmacro customInstallerFailed
+  FunctionEnd
+!endif
+
 !ifmacrodef customHeader
   !insertmacro customHeader
 !endif
diff --git a/node_modules/app-builder-lib/templates/nsis/multiUserUi.nsh b/node_modules/app-builder-lib/templates/nsis/multiUserUi.nsh
index 52a83df..0500e4e 100644
--- a/node_modules/app-builder-lib/templates/nsis/multiUserUi.nsh
+++ b/node_modules/app-builder-lib/templates/nsis/multiUserUi.nsh
@@ -36,6 +36,28 @@ Var isForceCurrentInstall
 			Abort
 		${endIf}

+		!ifndef BUILD_UNINSTALLER
+			# In-app update (--updated): reuse the mode of the existing installation
+			# instead of asking again -- the user already chose at first install.
+			# With both or neither modes present, fall through to the normal page.
+			${if} ${isUpdated}
+				${if} $hasPerUserInstallation == "1"
+				${andIf} $hasPerMachineInstallation == "0"
+					!insertmacro setInstallModePerUser
+					Abort
+				${elseIf} $hasPerUserInstallation == "0"
+				${andIf} $hasPerMachineInstallation == "1"
+					${IfNot} ${UAC_IsAdmin}
+						ShowWindow $HWNDPARENT ${SW_HIDE}
+						!insertmacro UAC_RunElevated
+						Quit
+					${endIf}
+					!insertmacro setInstallModePerAllUsers
+					Abort
+				${endIf}
+			${endif}
+		!endif
+
 		StrCpy $isForceMachineInstall "0"
 		StrCpy $isForceCurrentInstall "0"
 		!ifmacrodef customInstallmode
